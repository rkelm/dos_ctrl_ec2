* Create README file
* Test: Make instanceid.txt, multi instande compatible -> instanceid_<NAME>.txt
* Create volume-snapshot-creation batch file.
* Create send-any-command batch file. Show command output to batch file user.
	* Use aws ssm?
	* Can this be done easily at all?
* Is app shutdown executed safely at ec2 instance termination?
	* Add logging to app init-script to check. Is everything saved correctly? (Create log file with service script output (starting, stopping,...))
* Add batch file with save/backup command. Add to app service script.
* More error checking at instance termination cli calls.
